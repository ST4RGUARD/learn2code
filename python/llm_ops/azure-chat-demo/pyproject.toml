[project]
name = "azure-chat-demo"
version = "0.1.0"
requires-python = ">=3.13.2"
dependencies = [
    "azure-ai-inference>=1.0.0b9",
    "azure-search-documents",
    "ipykernel",
    "langchain",
    "python-dotenv",
    "semantic-kernel",
]
